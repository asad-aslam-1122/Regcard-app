enum LanguageEnum { english, french }

enum FileTypeEnum {
  pdf,
  doc,
  docx,
  xls,
  xlsx,
  png,
  jpg,
  jpeg,
}

enum ResetPasswordOptionEnum {
  otp,
  password,
}

enum DocumentType {
  myDocuments,
  familyDocuments,
  additionalDocuments,
}

enum HiveKeysEnum { authUser, bearer }

enum MaritalStatus { single, taken, married }

enum DeviceTypeEnums { androidMobile, androidTab, iOSMobile, iOSTab, web }

enum ConnectionRequestStatus { pending, accepted, rejected }

enum NotificationToggleEnum {
  chatReplies,
  connectionRequest,
  newsArticleUploaded,
  sharingHistory,
  sharingRequestConfirmation,
  shareRegCard,
  regCardFirstShare
}

enum MessageTypeEnum { image, video, article, text, location }

enum DurationType { hour, day, week, month, year }

enum PaymentStatusEnum { pending, accepted, rejected }

enum PaymentMethodEnum { fix, percentage }

enum PaymentSplitEnum { equally, custom }
